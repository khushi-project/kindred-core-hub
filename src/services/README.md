# Services Layer

Placeholder for future Convex backend integration. Each module exposes a
thin async API that currently returns mock data. When Convex is wired up,
replace the internals with `useQuery` / `useMutation` calls — the consuming
components do not need to change.